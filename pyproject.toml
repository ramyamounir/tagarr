[project]
name = "tagarr"
version = "0.1.0"
description = "Web UI for managing Sonarr and Radarr name aliases"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "gunicorn",
]
